import { FormateoMotivoPipe } from './formateo-motivo.pipe';

describe('FormateoMotivoPipe', () => {
  it('create an instance', () => {
    const pipe = new FormateoMotivoPipe();
    expect(pipe).toBeTruthy();
  });
});
